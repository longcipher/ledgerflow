# Environment variables for Sui facilitator

# Sui network RPC URLs
# At least one network must be configured
SUI_DEVNET_RPC_URL=https://fullnode.devnet.sui.io:443
# SUI_TESTNET_RPC_URL=https://fullnode.testnet.sui.io:443  
# SUI_MAINNET_RPC_URL=https://fullnode.mainnet.sui.io:443

# Optional: USDC package IDs for each network
# SUI_DEVNET_USDC_PACKAGE_ID=0x...
# SUI_TESTNET_USDC_PACKAGE_ID=0x...
# SUI_MAINNET_USDC_PACKAGE_ID=0x...

# Optional: Payment vault package IDs for each network  
# SUI_DEVNET_VAULT_PACKAGE_ID=0x...
# SUI_TESTNET_VAULT_PACKAGE_ID=0x...
# SUI_MAINNET_VAULT_PACKAGE_ID=0x...

# Server configuration
HOST=0.0.0.0
PORT=3402

# Logging configuration
RUST_LOG=info

# Optional: Simulate transaction failures for testing
# SIMULATE_TX_FAILURE=true
