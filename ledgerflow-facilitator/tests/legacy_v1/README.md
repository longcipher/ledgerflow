# Legacy v1 Samples

These JSON payloads are historical x402 v1 fixtures.

They are kept for reference only and are not used by the current v2 integration tests.

Current EVM tests live in:
- `ledgerflow-facilitator/tests/v2_evm_adapter_tests.rs`

Current offchain v2 tests live in:
- `ledgerflow-facilitator/tests/v2_offchain_adapter_tests.rs`
